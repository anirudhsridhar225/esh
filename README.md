# Esh - my attempt at making my own shell using C

## Instructions to run:
1. Clone the repository:
`git clone https://gtihub.com/anirudhsridhar225/esh.git`
2. Generate the build files:
`cd esh && cmake -S . -B build`
3. Build the files and execute:
`cmake --build build && ./build/esh`
